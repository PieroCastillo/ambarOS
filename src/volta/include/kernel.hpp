#ifndef AMBAROS_KERNEL_H
#define AMBAROS_KERNEL_H
/*
using Process = unsigned long;
using Thread = unsigned long;
using Mutex = unsigned long;
using File = unsigned long;
using Address = unsigned long;

enum ResultType
{
    Success,
    Error,
};

/// System calls

// Threading
void CreateProcess();
void RunProcess();
void DestroyProcess();

void CreateThreadPool();
void DestroyThreadPool();

void CreateThreads();
void DispatchThreads();
void DestroyThreads();

// Synchronization primitives
void CreateMutex(Mutex* mutex);
void DestroyMutex(Mutex* mutex);

// Memory management
void AllocateMemory(Address address);
void FreeMemory(Address address);

// I/O
void ReadFile();
void WriteFile();
void OpenFile();
void CloseFile();
*/
#endif